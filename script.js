window.onload=function(){
   var c= document.querySelector('canvas');
   var canvas= document.querySelector('canvas');
   c.width=innerWidth;
   c.height=innerHeight;
   c.getContext('2d')
   console.log('c',c.getContext('2d'))
   // start
}